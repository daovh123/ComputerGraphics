# ComputerGraphics

Ứng dụng React + Vite cho học liệu tương tác.

## Chạy local

1. Cài dependencies:
   `npm install`
2. Khởi động môi trường dev:
   `npm run dev`
3. Build production:
   `npm run build`
